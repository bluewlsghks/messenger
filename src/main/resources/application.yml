spring:
  data:
    mongodb:
      uri: ${MONGODB_URI}

server:
  port: 8080

app:
  crypto:
    aesKeyBase64: ${APP_AES_KEY_BASE64}
  jwt:
    secretBase64: ${APP_JWT_SECRET_BASE64}
    expiresMinutes: 120
  openai:
    api-key: ${OPENAI_API_KEY:}
